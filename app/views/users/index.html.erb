
<h1>Listing users</h1>

<table>
  <tr>
    <th>名称</th>
    <th>密码</th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.pwd %></td>
      <td><%= link_to 'Show', user_path(user) %></td>
    </tr>
  <% end %>
</table>
